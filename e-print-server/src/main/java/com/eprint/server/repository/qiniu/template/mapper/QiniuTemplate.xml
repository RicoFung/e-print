<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eprint.server.repository.qiniu.template.dao.QiniuTemplateDao">

    <resultMap type="com.eprint.server.repository.qiniu.template.model.result.QiniuTemplateResult" id="templateResult">
        <result property="id" column="ID"/>
        <result property="templateTypeId" column="TEMPLATE_TYPE_ID"/>
        <result property="templateType" column="TEMPLATE_TYPE"/>
        <result property="templateTypeName" column="TEMPLATE_TYPE_NAME"/>
        <result property="templateCode" column="CODE"/>
        <result property="bucketName" column="BUCKET_NAME"/>
        <result property="objectName" column="OBJECT_NAME"/>
        <result property="status" column="STATUS"/>
    </resultMap>

    <select id="getByTemplateCode"
            parameterType="com.eprint.server.repository.qiniu.template.model.param.QiniuTemplateGetByCodeParam"
            resultMap="templateResult">
        SELECT
            T.ID,
            T.TEMPLATE_TYPE_ID,
            TT.CODE AS TEMPLATE_TYPE,
            TT.NAME AS TEMPLATE_TYPE_NAME,
            T.CODE,
            T.BUCKET_NAME,
            T.OBJECT_NAME,
            T.STATUS
        FROM E_PRINT_QINIU_TEMPLATE T
        JOIN E_PRINT_QINIU_TEMPLATE_TYPE TT ON TT.ID = T.TEMPLATE_TYPE_ID
        WHERE TT.CODE = #{templateType}
          AND T.CODE = #{templateCode}
          AND T.STATUS = #{status}
          AND TT.STATUS = #{status}
    </select>
</mapper>
